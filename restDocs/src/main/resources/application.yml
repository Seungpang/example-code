spring:
  jpa:
    database: h2
    hibernate:
      ddl-auto: create
    show-sql: true
    open-in-view: false

management:
  endpoint:
    web:
      exposure:
        include:
          - "*"

logging:
  level:
    root: info